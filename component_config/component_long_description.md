The InfluxDB 2 Extractor transfers time-series data from InfluxDB v2 to Keboola Storage using custom Flux queries.

Key features:
- Sync actions for listing organizations and buckets
- Incremental and full load modes
- Dynamic table naming based on tag values
- Maintains schema consistency across runs